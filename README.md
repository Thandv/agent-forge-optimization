# agent-forge-optimization

Content bundle split from agent-forge (3 item(s)). Self-contained: run `python3 scripts/validate.py` to check the security + schema gate. Recombine with other bundles via the agent-forge builder (`builder/compose.py`).

See THIRD_PARTY.md for upstream attributions.
